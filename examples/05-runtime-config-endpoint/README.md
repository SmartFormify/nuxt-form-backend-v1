# Nuxt Runtime Config Form Endpoint

Declare `runtimeConfig.public.smartformifyEndpoint` and supply `NUXT_PUBLIC_SMARTFORMIFY_ENDPOINT`. Public runtime config is sent to the browser; it is configuration, not a way to hide an endpoint URL. See [the complete guide](../../docs/nuxt-runtime-config-form.md).
