# Nuxt Form Submission with $fetch

Use `$fetch` inside a submit event. `useFetch` is designed to run in setup, plugins, or middleware and is not the normal click-submit pattern. See [the focused guide](../../docs/nuxt-fetch-form.md).
