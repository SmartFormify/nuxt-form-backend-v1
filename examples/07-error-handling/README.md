# Nuxt Form Error Handling

The shared runnable form distinguishes a failed endpoint response from a transport error, disables duplicate submits while pending, and leaves the form data in place on failure.
