# Native Nuxt Form Validation

Use semantic input types and `required`, `minlength`, and `maxlength` for immediate browser feedback. Client validation is for usability and does not replace endpoint-side protections.
