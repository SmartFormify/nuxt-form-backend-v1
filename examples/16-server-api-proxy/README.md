# Nuxt Server Route Form Submission

`server/api/contact.post.ts` receives `/api/contact` and forwards the body. Use a route when your app needs server-side processing, auditing, or composition. A normal SmartFormify browser submission does not require a proxy and a proxy does not make the public endpoint URL secret.
